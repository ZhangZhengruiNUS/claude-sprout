# Next Session Handoff

Updated: 2026-05-20 14:54:59 Asia/Shanghai
Generated by: `scripts/update-handoff.mjs`

## Resume Protocol

When a new Codex session starts in this repository:

1. Read `AGENTS.md`.
2. Read this file.
3. Skim `docs/devlog.md`.
4. Run `git status --short --branch` and compare with the snapshot below.
5. If the user says "continue" or gives a new Claude Sprout requirement, continue from "Next Tasks" unless newer user instructions override it.

## Git Snapshot

Latest commit at generation time:

```text
73ffd9d fix: make pet window draggable
```

Branch status at generation time:

```text
## main...origin/main
M  docs/devlog.md
M  docs/handoff-state.json
M  docs/next-session.md
M  package-lock.json
M  package.json
M  src-tauri/capabilities/default.json
A  src-tauri/src/app_settings.rs
M  src-tauri/src/commands.rs
M  src-tauri/src/lib.rs
M  src/App.tsx
M  src/pet/PetRenderer.tsx
M  src/pet/petWindowControls.ts
M  src/settings/SettingsPanel.tsx
A  src/settings/appSettings.test.ts
A  src/settings/appSettings.ts
A  src/settings/appSettingsApi.ts
M  src/styles/app.css
```

Recent commits:

```text
73ffd9d (HEAD -> main, origin/main) fix: make pet window draggable
4ea863d docs: automate handoff updates
2fef4c0 docs: add project continuity handoff
8326a6a feat: add interactive resizable pet window
f3e7863 chore: enable tauri desktop build
```

Remotes:

```text
origin	https://github.com/ZhangZhengruiNUS/claude-sprout.git (fetch)
origin	https://github.com/ZhangZhengruiNUS/claude-sprout.git (push)
```

## Current State

- Claude Sprout is a public GitHub repository at https://github.com/ZhangZhengruiNUS/claude-sprout.
- The app is a Tauri v2 + React + TypeScript + Rust desktop companion for Claude Code.
- The primary surface is a transparent, frameless, always-on-top pet window. The full session panel is secondary and opens from the pet or tray.

## Implemented So Far

- React/Vite frontend scaffold
- Tauri v2 Rust backend scaffold
- Tray skeleton
- Main session panel with mock sessions
- PowerShell and Node hook/statusline writers
- Local session state model and stale/probably_closed TTL tests
- Codex-compatible pet importer Rust skeleton
- Tauri release exe build support
- Interactive pet window: click opens panel, drag moves window through manual setPosition tracking, wheel resizes, right-click enlarges, double-click waves, scale persists in localStorage, transparent background, shadow disabled
- Settings persistence under %USERPROFILE%\.claude-sprout\settings.json for do-not-disturb, pet always-on-top, pet lock-position, and pet size
- Settings UI controls for small/medium/large pet size, always-on-top, do-not-disturb, and lock-position; changes sync to the live pet window
- Vitest test runner with app settings schema tests, plus Rust tests for app-data settings load/save/clamp behavior
- Repository continuity files: AGENTS.md, docs/next-session.md, docs/devlog.md
- Handoff updater script and optional git pre-commit hook installer

## Verification Commands

Known passing commands:

```powershell
npm test
npm run lint
npm run build
cargo test --manifest-path src-tauri\Cargo.toml
npm run tauri -- build --no-bundle
```

Package scripts:

- `dev`: `vite`
- `tauri`: `tauri`
- `tauri:dev`: `tauri dev`
- `tauri:build`: `tauri build`
- `build`: `tsc -b && vite build`
- `lint`: `eslint .`
- `test`: `vitest run`
- `preview`: `vite preview`
- `handoff:update`: `node scripts/update-handoff.mjs`
- `hooks:install`: `powershell -NoProfile -ExecutionPolicy Bypass -File scripts/install-dev-hooks.ps1`

Run the desktop exe:

```powershell
.\src-tauri\target\release\claude-sprout.exe
```

Run the dev preview:

```powershell
npm run dev
```

Pet preview route:

```text
http://127.0.0.1:5173/?window=pet
```

## Known Blockers

- Full MSI bundling with npm run tauri:build fails at WiX download/install on this machine: Tauri auto-download of WiX hit TLS UnknownIssuer, and winget install WiXToolset.WiXToolset requires admin rights to enable NetFx3.
- The release exe build works with npm run tauri -- build --no-bundle.

## Next Tasks

1. Improve Codex-like pet behavior: add idle/wave/run/jump/failed/review animation controller, support imported codex-8x9 spritesheets in the pet window, and add pet preview/apply UI.
2. Wire real session refresh: file watcher with debounce, native notifications for waiting_permission/waiting_input/done/error, tray menu actions for refresh/open data/settings.
3. Connect persisted do-not-disturb to notifications and tray state instead of only the panel UI.
4. Improve installer path: document WiX prerequisite or add an NSIS target if it avoids the current WiX blocker.

## Important Decisions

- Do not make the product a browser-style dashboard. The pet window is the first-class UI.
- Do not auto-approve Claude Code permission prompts.
- Keep prompts/outputs out of stored state by default.
- PowerShell hook writer is the default for Windows; Node hook writer remains a fallback.
- Imported Codex-compatible pet assets are copied into Claude Sprout app data; do not reference or distribute official Codex assets.

## Update Mechanism

This file is generated from `docs/handoff-state.json` plus live git/package metadata.

Use:

```powershell
npm run handoff:update
```

Optional local automation:

```powershell
npm run hooks:install
```

The installed git pre-commit hook runs `npm run handoff:update` and stages `docs/next-session.md` automatically before each commit.
